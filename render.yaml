## ─────────────────────────────────────────────────────────────────────────────
## Render Blueprint — AI Task Queue
## Deploy everything at once: https://dashboard.render.com/blueprints
## ─────────────────────────────────────────────────────────────────────────────

services:

  # ── Key Value (Redis) ───────────────────────────────────────────────────────
  - type: keyvalue
    name: aiq-redis
    plan: free
    maxmemoryPolicy: noeviction
    ipAllowList: []   # internal only — no external access needed

  # ── 1. API Gateway (public-facing) ─────────────────────────────────────────
  - type: web
    name: aiq-api-gateway
    runtime: docker
    dockerfilePath: ./api-gateway/Dockerfile
    dockerContext: ./api-gateway
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: CORE_SERVICE_URL
        value: https://aiq-core-service.onrender.com
      - key: WORKER_SERVICE_URL
        value: https://aiq-worker-service.onrender.com
      - key: FRONTEND_URL
        value: https://aiq-frontend.vercel.app   # update after Vercel deploy

  # ── 2. Core Service ─────────────────────────────────────────────────────────
  - type: web
    name: aiq-core-service
    runtime: docker
    dockerfilePath: ./core-service/Dockerfile
    dockerContext: ./core-service
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      # Postgres — injected from managed DB
      - key: PGHOST
        fromDatabase:
          name: aiq-postgres
          property: host
      - key: PGPORT
        fromDatabase:
          name: aiq-postgres
          property: port
      - key: PGDATABASE
        fromDatabase:
          name: aiq-postgres
          property: database
      - key: PGUSER
        fromDatabase:
          name: aiq-postgres
          property: user
      - key: PGPASSWORD
        fromDatabase:
          name: aiq-postgres
          property: password
      # Redis
      - key: REDIS_URL
        fromService:
          name: aiq-redis
          type: keyvalue
          property: connectionString
      - key: RABBITMQ_HOST
        sync: false
      - key: RABBITMQ_PORT
        value: "5672"
      - key: RABBITMQ_DEFAULT_USER
        sync: false
      - key: RABBITMQ_DEFAULT_PASS
        sync: false
      - key: RABBITMQ_VHOST
        sync: false
      # Secrets — enter values during blueprint setup
      - key: JWT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false

  # ── 3. Worker Service (runs as web service on free plan) ────────────────────
  - type: web
    name: aiq-worker-service
    runtime: docker
    dockerfilePath: ./worker-service/Dockerfile
    dockerContext: ./worker-service
    plan: free
    healthCheckPath: /actuator/health
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      # Redis
      - key: REDIS_URL
        fromService:
          name: aiq-redis
          type: keyvalue
          property: connectionString
      # RabbitMQ — enter values during blueprint setup
      - key: RABBITMQ_HOST
        sync: false
      - key: RABBITMQ_PORT
        value: "5672"
      - key: RABBITMQ_DEFAULT_USER
        sync: false
      - key: RABBITMQ_DEFAULT_PASS
        sync: false
      - key: RABBITMQ_VHOST
        sync: false
      # Secrets — enter values during blueprint setup
      - key: OPENAI_API_KEY
        sync: false

# ── Postgres ───────────────────────────────────────────────────────────────────
databases:
  - name: aiq-postgres
    plan: free
    databaseName: ai_task_queue
    user: admin
