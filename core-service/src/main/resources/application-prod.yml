spring:
  config:
    import: ""   # no .env file in production — use real env vars
  datasource:
    # Render injects PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD for managed Postgres
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  redis:
    # Fixed Redis configuration for Render
    host: red-d7mv0orbc2fs738grg0g
    port: 6379
  rabbitmq:
    host: ${RABBITMQ_HOST}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_DEFAULT_USER}
    password: ${RABBITMQ_DEFAULT_PASS}
    virtual-host: ${RABBITMQ_VHOST:jbkdkvkb}

logging:
  file:
    name: ""   # no file logging in prod — stdout only (Render captures stdout)
